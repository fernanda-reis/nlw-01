import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// problemas com tipos react
// npm install @types/react -D

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

